using LaundryPOS.Forms.Views;
using System;
using System.Collections;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace LaundryPOS.DAL
{
    public class ReceiptDataService
    {
        //public DataSet CreateReceiptDataSet(List<CartItem> items)
        //{
        //    DataSet dataSet = new DataSet("OrderDataSet");
        //    DataTable dataTable = new DataTable("ReceiptDataTable");

        //    dataTable.Columns.Add("ID", typeof(int));
        //    dataTable.Columns.Add("Name", typeof(string));
        //    foreach (var item in items)
        //    {
        //        DataRow row = dataTable.NewRow();
        //        row["ID"] = item.ID;
        //        row["Name"] = item.Name;
        //        dataTable.Rows.Add(row);
        //    }

        //    Add the DataTable to the DataSet
        //    dataSet.Tables.Add(dataTable);

        //    return dataSet;
        //}
    }
}